---
# Global variables for all hosts
# Role-specific variables are defined in each role's defaults/main.yml

device_user: root

# OpenWrt Build Server Configuration
# Configure these to point to your remote build server
build_server_host: openmanet.marmal.duckdns.org
build_server_user: ubuntu
build_ssh_key: ~/.ssh/id_rsa_proxmox_vms
build_server_path: /home/ubuntu/source/firmware/

